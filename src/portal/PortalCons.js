// const values for Portal module

const baseUrl = '/portal'
const portalUrls = [baseUrl + '/group',
  baseUrl + '/personal',
  baseUrl + '/newEmployee',
  baseUrl + '/area']

export default{
  PORTAL_URLS: portalUrls
}
